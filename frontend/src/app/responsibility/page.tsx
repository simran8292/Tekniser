import type { Metadata } from "next";
import ResponsibilityContent from "./ResponsibilityContent";

export const metadata: Metadata = {
  title: "Corporate Responsibility & ESG — TAKNISER ONE GLOBE",
  description:
    "Explore TAKNISER ONE GLOBE's commitment to corporate responsibility, ethical governance, environmental sustainability, and the global TAKNISER family.",
};

export default function ResponsibilityPage() {
  return <ResponsibilityContent />;
}
